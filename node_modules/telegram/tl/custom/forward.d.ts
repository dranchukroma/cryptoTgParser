import { ChatGetter } from "telegram/tl/custom/chatGetter";
import { SenderGetter } from "telegram/tl/custom/senderGetter";
import { Api } from "telegram/tl/api";
import type { TelegramClient } from "telegram/client/TelegramClient";
import type { Entity } from "telegram/define";
import { inspect } from "telegram/inspect";
export declare class Forward extends SenderGetter {
    private originalFwd;
    [inspect.custom](): {
        [key: string]: any;
    };
    constructor(client: TelegramClient, original: Api.MessageFwdHeader, entities: Map<string, Entity>);
}
export interface Forward extends ChatGetter, SenderGetter {
}
