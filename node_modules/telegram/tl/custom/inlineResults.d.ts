import type { TelegramClient } from "telegram";
import type { EntityLike } from "telegram/define";
import { Api } from "telegram/tl/api";
import { InlineResult } from "telegram/tl/custom/inlineResult";
import { inspect } from "telegram/inspect";
export declare class InlineResults extends Array<InlineResult> {
    private result;
    private queryId;
    private readonly cacheTime;
    private readonly _validUntil;
    private users;
    private gallery;
    private nextOffset;
    private switchPm;
    [inspect.custom](): {
        [key: string]: any;
    };
    constructor(client: TelegramClient, original: Api.messages.TypeBotResults, entity?: EntityLike);
    resultsValid(): boolean;
}
